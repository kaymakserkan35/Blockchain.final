<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.betelgeuse.blockchain">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:theme="@style/Theme.Blockchain">
        <activity
            android:name=".userinterface.analysis.AnalysisActivity"
            android:exported="false" />
        <activity
            android:name=".userinterface.useroption.UserOptionActivity"
            android:exported="false" />
        <activity
            android:name=".userinterface.authentication.AuthenticationActivity"
            android:exported="true"
            android:label="@string/Blockchain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".userinterface.information.InformationActivity"
            android:exported="false" />
    </application>

</manifest>